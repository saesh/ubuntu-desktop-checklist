# fonts.conf

To enable full emoji support in Alacritty a custom font family `iosevkaemoji` is created which can be used in Alacrittys configuration. The font family merged a standard font with an emoji font like `Noto Color Emoji`.